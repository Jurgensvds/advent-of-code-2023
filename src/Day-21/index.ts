import { readTextFile } from "../file-reader";

export function main(filePath = 'src/Day-21/input.txt') {
    readTextFile(filePath, false).then((values) => {
        // Start Here
    });
}

// Part One - START

function partOne() {
    const result = 'TODO';
    console.log(`Part One: ${result}`);
}

// Part One - END
// -----------------------------------------------------------------------------------------------
// Part Two - START

function partTwo() {
    const result = 'TODO';
    console.log(`Part Two: ${result}`);
}

// Part Two - END

// -----------------------------------------------------------------------------------------------

// Utility Functions